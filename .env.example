# Environment variables for Malaya Web
# Do NOT commit real API keys; this file is safe to commit.
# Copy to .env.local for local development or set on your hosting provider for production.

# Resend API Key (server-side only)
RESEND_API_KEY="your-resend-api-key"

# Public form endpoint used by Contact.astro
PUBLIC_FORM_ENDPOINT="/api/contact"

# Notes:
# - Astro/Vite will load .env, .env.local and mode-specific files like .env.development, .env.production.
# - Variables prefixed with PUBLIC_ are exposed to the client; keep secrets like RESEND_API_KEY unprefixed.
